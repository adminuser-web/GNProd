export const FEATURES = {
  savedBuilds: true,
  aiBatConsultant: true,
  supportRequests: true,
  // Razorpay payment-link gateway. Keep OFF until the edge functions are
  // deployed and the Razorpay keys/webhook are configured & tested.
  paymentGateway: false,
};
